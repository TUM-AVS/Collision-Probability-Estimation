# Collision-Probability-Estimation

Methods for calculating the overlap probability are within the **coll_state_overlap** file. A script running them is provided in **scripts_overlap.py**


Methods for calculating the collicions probability along trajectories are within the **coll_strajectories.py** file. A script running them is provided in **scripts_overlap.py**